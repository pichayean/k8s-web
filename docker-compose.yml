version: "3.9"

services:
  k8s-web:
    build: .
    ports:
      - "4000:4000"
    volumes:
      - ./templates:/app/templates
      - ~/.kube:/root/.kube:ro  # ใช้ kubeconfig host
    environment:
      - KUBECONFIG=/root/.kube/config
