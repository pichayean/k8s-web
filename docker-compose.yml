version: "3.9"

services:
  k8s-web:
    build: .
    ports:
      - "4000:4000"
    volumes:
      - ~/.kube:/root/.kube  # <--- mount kube config
      - ~/.minikube:/root/.minikube  # <--- สำหรับ Minikube
    environment:
      - KUBECONFIG=/root/.kube/config
